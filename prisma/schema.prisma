generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgres://api_todolist_r1j5_user:bLTAgo4jKukdmIXTV8qfYEHBJE67AGIH@dpg-cj58118eba7s73ecml00-a/api_todolist_r1j5")
}

model tarefas {
  idtarefas  Int    @id @default(autoincrement())
  tarefa     String @db.VarChar(45)
  prioridade String @db.VarChar(45)
}

model concluidos {
  idconcluidos Int    @id @default(autoincrement())
  tarefa       String @db.VarChar(45)
  prioridade   String @db.VarChar(45)
}
